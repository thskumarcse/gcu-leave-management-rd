Place import files here. They are not committed because they contain employee records.

- `employee.csv` — used by `python manage.py import_employees`
- `emp_leaves.xls` — used by `python manage.py import_emp_leaves`
